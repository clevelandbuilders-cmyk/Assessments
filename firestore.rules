rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Any authenticated user (your team) can read/write everything.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
